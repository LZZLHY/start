DATABASE_URL="postgresql://start:start@localhost:5432/start?schema=public"
PORT=3100

# ============================================
# JWT_SECRET - JWT 签名密钥（重要！）
# ============================================
# 用于用户登录 token 的签名和验证
# 
# ⚠️ 安全警告：
# - 生产环境必须修改为随机字符串（至少 32 字符）
# - 不要使用默认值，否则任何人都可以伪造登录 token
# - 修改后所有已登录用户需要重新登录
#
# 生成随机密钥的方法：
# - Linux/Mac: openssl rand -base64 32
# - PowerShell: [Convert]::ToBase64String((1..32|%{Get-Random -Max 256})-as[byte[]])
# - 或使用在线工具生成 UUID 拼接
#
JWT_SECRET="please-change-me-to-random-string"

# HOST 配置说明：
# - 不设置时自动检测：Windows 开发环境用 127.0.0.1，Linux/生产环境用 0.0.0.0
# - 生产部署时设置 NODE_ENV=production 会自动使用 0.0.0.0
# - 如需手动指定，取消下面的注释：
# HOST=0.0.0.0


